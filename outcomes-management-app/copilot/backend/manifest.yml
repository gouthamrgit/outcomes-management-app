name: backend
type: Backend Service

image:
  build: backend/Dockerfile

cpu: 256
memory: 512
count: 1

variables:
  DYNAMODB_TABLE_NAME: MyOutcomesTable
  AWS_REGION: us-west-2

addons:
  - dynamodb.yml

environments:
  dev:
    variables:
      DYNAMODB_TABLE_NAME: MyOutcomesTable