name: frontend
type: Load Balanced Web Service

image:
  build: frontend/Dockerfile
  port: 3000

http:
  path: '/'

cpu: 256
memory: 512
count: 1

environments:
  dev:
    variables:
      REACT_APP_API_URL: http://backend.dev.outcomes-management-app.locals
